#include<iostream>
#include<iomanip>
using namespace std;

int main()
{
	int n, boi;
	cout << "Nhap so " << endl;
	cin >> n;
	cout << "Boi so cua n voi cac so tu 1 den 20 la " << endl;
	cout << "\n";

	for (int i = 1; i <= 20; i++) {
		cout << setw(5) << n * i;
	}
}
