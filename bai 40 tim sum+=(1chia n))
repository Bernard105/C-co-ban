#include<iostream>
using namespace std;

int main()
{
	int n;
	cout << "Nhap so" << endl;
	cin >> n;
	double sum = 0;

	for (int i = 1; i <= n; i++) {
		sum += (1.0 / i);
	}

	cout << "Dap so " << sum << endl;
	
	return 0;

}
