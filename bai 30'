#include<iostream>
using namespace std;

int main()
{
	int i;
	bool flag = true;
	cout << "Nhap so" << endl;
	cin >> i;

	do {
		if (i >= 1 && i <= 100) {
			break;
		}
		else {
			flag = false;
			cout << "Vui long nhap so khac " << endl;
			cin >> i;
		}
	} while (true);
}
