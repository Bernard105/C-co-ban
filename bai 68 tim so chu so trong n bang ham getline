#include<iostream>
#include<string>

using namespace std;

int main()
{
    string s;
    getline(cin, n);
    for(int i = 0; i < s.length; i++)
    {
        if(s[i] == "-")
        {
            s.erase(i, 1);
            i--;
        }
    }
    
    cout << s.length();
    
    return 0;
}
